# Copy to .env.local and fill in. Vite exposes only VITE_* vars to the browser.

# No frontend env vars required for local dev — the Vite proxy handles
# routing /api/* to the local backend services automatically.
